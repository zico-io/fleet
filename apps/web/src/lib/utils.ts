// The canonical `cn` lives in the shared design system (@zico/ui). Re-exported
// here so the shadcn `utils` alias (components.json) keeps resolving and app
// code can keep importing from "@/lib/utils".
export { cn } from "@zico/ui/cn";
